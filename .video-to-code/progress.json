{
  "url": "https://youtu.be/Ak1ACaqNtM0",
  "title": "AI SaaS - Sketch To Design | CodeRabbit, Convex, Inngest, Sevalla, Polar",
  "completed": [
    2,
    4,
    5,
    7,
    8,
    9,
    12,
    13,
    15,
    17,
    18,
    19,
    20,
    21,
    23
  ],
  "current": 24,
  "deferred": {
    "3": "Deploy on Sevalla. Opens 'is sponsored by Sevalla' [21:18] and needs a Sevalla account. Deploying an empty skeleton can wait until there is something to deploy.",
    "6": "Design & theming. He pastes a pre-made globals.css from his Figma design [91:04]; the colour values are never shown on screen. His own fallback at [88:57] is to grab a shadcn theme, which is what the project currently has.",
    "10": "GitHub commits. Version-history walkthrough, zero code-writing lines.",
    "11": "CodeRabbit AI code reviewer. Tool install demo, zero code-writing lines.",
    "16": "How to use CodeRabbit in VSC/Cursor. Tool demo.",
    "22": "Introduction to Background Jobs with Inngest. Conceptual explainer - zero code-writing lines, verified."
  },
  "notes": [
    "Honoured the tutorial's Next.js 15.4.6 pin [15:26] rather than upgrading; the rest of the course depends on it.",
    "shadcn CLI changed since recording: init no longer takes --base-color and now prompts for theme presets. Wrote components.json directly with baseColor=neutral, the value asked for at [17:15].",
    "shadcn add --all fails on a broken registry item (questionnaire.json 404s). Installed the canonical 46 components explicitly - same outcome as the 'hit A' trick at [18:02].",
    "Skipping init meant src/lib/utils.ts, the clsx/tailwind-merge deps and the entire theme token block were never generated. All added by hand; the missing tokens are why the first render of the auth pages was unstyled.",
    "Added suppressHydrationWarning on <html>; next-themes requires it and the video does not mention it.",
    "Auth routes are a literal src/app/auth/ folder, not a route group. Confirmed from the file tree on screen at 27:35 - this is why /sign-up 404s for him at [27:32] and he corrects to /auth/sign-up.",
    "Tailark blocks could not be installed via CLI (same shadcn version drift he hits at [26:19]). Rebuilt the equivalent layout from the installed shadcn primitives, matching the structure read off the frame.",
    "Convex Auth: latest @convex-dev/auth@0.0.94 requires @auth/core ^0.41.1 and conflicts with the tutorial pin. Pinned both to the tutorial versions (@convex-dev/auth@0.0.88, @auth/core@0.37.0), which also matches the setup CLI that was run.",
    "convex must be a project dependency before npx convex dev will run; installed convex@1.25.4 per the pin at [34:20].",
    "Middleware lives at src/middleware.ts, not the repo root - he stresses this at [54:38].",
    "Convex Auth was created with providers: [] - nothing could sign in until the Password provider was added, which the tutorial does later in ch5 around [67:01].",
    "The Tailark sign-up block has no email field, but Password auth identifies users by email. Added an Email input; without it sign-up cannot work.",
    "combineReducers({}) throws \"Store does not have a valid reducer\" at runtime, so the empty slices record he sets up first is not viable on its own. Added the profile slice from [150:26] to make the store valid.",
    "The projects schema is never narrated - he pastes it. Read the exact columns off the Convex dashboard on screen at [180:03]: name, projectNumber, createdAt, lastModified, isPublic, sketchesData, description, generatedDesignData, inspirationImages, moodBoardImages, styleGuide, tags. His table list also shows credits_grants, credits_ledger, project_counters and subscriptions, which later chapters add.",
    "Used a project_counters table for auto-naming rather than counting rows, so deleting a project cannot make a later one reuse its number.",
    "The dashboard UI is never narrated in detail. Read it off frames at ~205:30: circular logo, TODO:credits, help icon, avatar and a rounded-full New Project pill in the navbar; Your Projects heading with the subtitle Manage your design projects and continue where you left off; and a grid of warm gradient preview tiles with a soft white disc centred, name and relative time underneath. No card borders - the tile is the card.",
    "Frame at ~205:30 also showed his page composition: const Page = async () => { const { projects, profile } = await ProjectsQuery() } with an Authentication Required fallback, then <ProjectsProvider initialProjects={projects}><div className=\"container mx-auto py-36 px-4\"><ProjectsList /></div></ProjectsProvider>. Rebuilt to match rather than the client-only grid I had first.",
    "Frames also revealed the route structure still to come: src/app/(protected)/dashboard/[session]/(workspace)/ with layout.tsx, page.tsx and style-guide/page.tsx.",
    "Frame at ~206:50 showed his Figma for the workspace: the navbar switches modes in project context - breadcrumb Project / Name, centred Canvas and Style Guide tabs, and the button becomes New Frame instead of New Project. That is why the navbar reads useSearchParams().get(project).",
    "Canvas is a query param, not a path segment: /dashboard/[session]/canvas?project=<id>.",
    "Same frame showed the Style Guide design for ch13: Colours / Typography / Moodboard tabs, a Themes dropdown (Monochrome), then swatch groups - Primary Colours, Secondary and Accent, UI Component, Utility and Form, Status and Feedback - each swatch a chip with a hex. His Figma text styles: h1 38/40, h2 30/36, h3 24/32, h4 20/28, paragraph 16/28, lead 20/28, large 18/28, small 14/20, muted 14/20, x-small 12/18, xxs 10/14, xxxs 8/14.",
    "Ch13 design read from frames at ~225:40: heading and subtitle on the left with Colours/Typography/Moodboard tabs on the right of the same row; Themes dropdown set to Monochrome; five swatch groups; and a Typography tab with a Font selector reading Aa Manrope followed by seven weight specimens of the line Whereas disregard and contempt for human rights have resulted.",
    "getComputedStyle returns oklch tokens as lab() in Chrome, which is useless in a style guide. Painting each colour into a 1x1 canvas and reading the pixel back normalises any notation to hex.",
    "Frame at ~279:30 corrected ch13: his real style guide centres the heading, uses the subtitle \"Manage your style guide for your project.\" and lowercase centred tabs (colours / typography / moodboard) - not the left-aligned Figma layout with right-side tabs. Updated to match.",
    "Moodboard scatter algorithm read verbatim from the same frame: seed = sum of char codes of image.id; random = ((seed * 9301 + 49297) % 233280) / 233280; imageWidth 192 (w-48), overlapAmount 30, spacing 162; zIndex index+1; marginLeft -80px, marginTop -96px; container relative w-full max-w-[700px] h-[300px] mx-auto inside hidden lg:flex absolute inset-0.",
    "His component path is src/components/style/mood-board/index.tsx and the hook is use-styles.ts, per the editor tabs in the frame.",
    "Frame at ~376:40 gave ch17 structure: the hook is useInfiniteCanvas in src/hooks/use-canvas.ts (842 lines in his build), ZoomBar lives at src/components/canvas/toolbar/zoom/index.tsx, shapes use an RTK entity adapter (entityState.entities) and the viewport is { scale, translate }.",
    "attachCanvasRef is a callback ref that adds the wheel listener with { passive: false } - React onWheel is always passive, so preventDefault would not work and the page would scroll instead of zooming.",
    "The same frame captured a runtime error worth avoiding: calling useInfiniteCanvas from the canvas layout without use client fails with \"Attempted to call useInfiniteCanvas() from the server\".",
    "Canvas collapsed to height 0 on first run: h-full cannot resolve a percentage against a flex-1 parent with no definite height. Absolute inset-0 fixes it. Caught by measuring the element rect rather than trusting the screenshot.",
    "Ch18 toolbar read from a magnified frame crop at ~388:20: one centred bar holding select, hand, frame, rectangle, circle, pencil and arrow, with the zoom group nested inside the same bar as its own pill - not a separate control at the right edge as first built.",
    "An svg sized 0x0 paints nothing even with overflow visible, and the transformed layer has no intrinsic size to inherit. Path shapes anchor their svg to the shape bounding box and draw relative to it.",
    "Freehand needs its box computed from every sampled point. Deriving it from the start and the current cursor only clips the stroke into fragments, which is what happened on the first pencil render.",
    "Frame at ~453:00 shows the FINAL canvas layout, which differs from ch18: undo/redo bottom-left, the nine tools centred (select, frame, rect, circle, pencil, arrow, line, text, eraser), and zoom moved back out to the bottom-right. Frames carry Inspiration and Generate pills above their top-right corner.",
    "Undo appeared broken but was not: Next.js dev indicator (NEXTJS-PORTAL) sits in the bottom-left corner and swallowed the click. Confirmed with elementFromPoint, then offset the history cluster. Dev-only, but it makes the button unusable while developing.",
    "PARTIAL on 19 and 21: the properties/inspector panel (ch19) and the per-frame chat window (ch21) are not built. Both are tied to the AI chapters and need the Anthropic key.",
    "Ch23 autosave: debounced Convex mutation updateProjectSketches patching sketchesData and lastModified, a lastSaved serialisation compared before writing so no-op saves do not touch lastModified, and a save-status indicator. Shapes hydrate from the stored sketch on mount, with the baseline recorded so hydration is not mistaken for an edit.",
    "PARTIAL on 23: he routes autosave through an Inngest event (project/autosave.requested at [470:46]) and the chapter continues into Polar setup at [494:41]. Both need accounts. The Convex persistence path is built and verified without them.",
    "Anthropic wired up: @anthropic-ai/sdk 0.116.0, src/lib/anthropic.ts, POST /api/ai streams text. Model claude-opus-5.",
    "Gateway agentrouter.org works but 401s 'unauthorized_client_error' unless User-Agent is claude-cli/*; ANTHROPIC_CLIENT_UA supplies it. Auth header can be x-api-key or Bearer, either is fine. Responses carry an extra top-level 'billing' key the SDK ignores.",
    "Verified through the gateway: SSE streaming, adaptive thinking with display=summarized, output_config.effort, and both claude-opus-5 and claude-opus-4-8."
  ],
  "blocked": [
    "Google OAuth still not set up: needs a Google Cloud OAuth client (AUTH_GOOGLE_ID / AUTH_GOOGLE_SECRET) plus ngrok, since Google will not call localhost [40:21]. The Google and Microsoft buttons are inert. Email/password works.",
    ".env contents [16:02] come from the creator downloadable kit and are not shown on screen.",
    "Chapters 24 and 26-30 will need Polar, Inngest and an Anthropic API key."
  ]
}
