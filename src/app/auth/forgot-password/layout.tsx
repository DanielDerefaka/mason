export const metadata = { title: 'Reset your password | Mason' }

/** Exists only to give the client page below it a title. */
export default function ForgotPasswordLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
